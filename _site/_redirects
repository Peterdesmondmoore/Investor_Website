# Always serve the login page for any /login/* path
/login/*   /login/index.html   200

# Allow logged-in users with role "member" through
/*         /:splat             200!  Role=member

# Everyone else goes to login (path is preserved after /login/)
 /*        /login/:splat       302!