# Always serve the login page for any /login/* path
/login/*   /login/index.html   200

# Allow any logged-in user through (no role required)
/*         /:splat             200!  Force=true

# Everyone else goes to login
/*         /login/:splat       302!
