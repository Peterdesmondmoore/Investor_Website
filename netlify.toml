# Netlify build configuration
[build]
  # The command to run to build your site
  command = "npm ci && npm run build"
  
  # The folder that contains the finished site
  publish = "_site"

  [build.environment]
    NODE_VERSION = "20"
